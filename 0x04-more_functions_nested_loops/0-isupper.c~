#include <stdio.h>

/**
 * main - check the code.
 *
 * @a: alphabet input
 * _isupper for cheak
 * Return: 1 if upper, 0 else
 */
int _isupper(int a)
{

if (a >= 65 && a <= 90)
{
return (0);
}
else
{
return (1);
}
}
