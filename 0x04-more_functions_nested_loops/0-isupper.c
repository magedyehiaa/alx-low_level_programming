#include <main.h>

/**
 * _isupper - check the code, m e g o
 *
 * @a: alphabet input
 * _isupper for cheak
 * Return: 1 if upper, 0 else
 */
int _isupper(int a)
{
if (a >= 65 && a <= 90)
return (1);
else
return (0);
}
