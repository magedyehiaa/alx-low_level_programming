#include "main.h"
/**
 * is palindrome - reversed equal M E G oO o z
 *
 * @s: the char
 * Return: 1 if pal 0 if not
 */

int is_palindrome(char *s);
{
char *strrev(char *s);
if (s == strrev(char *s))
return (1);
else
return (0);
}
