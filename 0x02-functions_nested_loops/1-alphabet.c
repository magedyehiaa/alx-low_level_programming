#include "main.h"
/**
 * print_alphabet - utilizes on _putchar
 * print alphabet from a to z
*/
void print_alphabet(void)
{
int M;
for (M = 'a'; M <= 'z'; M++)
_putchar(M);
_putchar('\n');
return (0);
}
