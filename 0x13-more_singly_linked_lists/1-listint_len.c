#include "lists.h"

/**
 * listint_len - printing all elements "Megoo"
 * @h: first node
 * Return: no of modes as seen
 */
size_t listint_len(const listint_t *h)
{
size_t m = 0;
while (h)
{
printf("%d\n", h->n)
	h = h->next;
	m++;
}
return (i);
}
