#include <stdio.h>
/**
  * main - Entry point
  *
  * Description: 'reverse l alphabet mego'
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
char m = 'z';
while (m >= 'a')
{
putchar('m');
m--;
}
putchar('\n');
return (0);
}
