#include <stdio.h>
/**
  * main - Entry point
  *
  * Description: 'putchar pring alphabet L Then H, mego'
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
char m = 'a';
while (m <= 'z')
{
    if (m == 'e' || m == 'q')
m++;
putchar(m);
m++;
}
putchar('\n');
return (0);
}
