#include <stdio.h>
/**
  * main - Entry point
  *
  * Description: 'print from 0 to 9, mego'
  *
  * Return: Always 0 (Success)
  */
int main(void)
{  
int m = 0;
while (m < 10)
{
printf("%i", m);
m++;
}
putchar('\n');
return (0);
}
